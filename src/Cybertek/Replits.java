package Cybertek;

import com.sun.org.apache.xpath.internal.axes.PredicatedNodeTest;

import javax.imageio.stream.ImageInputStream;
import java.net.Inet4Address;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Scanner;

public class Replits {
    //your code here
//    static int countPos = 0;
//    static int countNeg = 0;
//    static int countZero = 0;
//
//    public static void plus_minus(int[] arr) {
//        for (int i = 0; i < arr.length; i++) {
//            if (arr[i] > 0) {
//                countPos++;
//            } else if (arr[i] < 0) {
//                countNeg++;
//            } else {
//                countZero++;
//            }
//        }
//        System.out.print("positives:" + countPos + ", ");
//        System.out.print("negatives:" + countNeg + ", ");
//        System.out.print("zeros:" + countZero);
//    }
//
//    public static void main(String[] args) {
//        Scanner inp = new Scanner(System.in);
//        int size = inp.nextInt();
//        int[] arr = new int[size];
//        for (int i = 0; i <= size - 1; i++) {
//            arr[i] = inp.nextInt();
//        }
//        plus_minus(arr);
//    }//end main
//}

//    public static void main(String[] args) {
//        Scanner inp = new Scanner(System.in);
//        String s = inp.next();
//        person(s);
//    }
//
//    public static void person(String info) {
//
//        //your code here
//        String[] personArr = info.split(",");
//
//            System.out.print("person name: " + personArr[0]);
//            System.out.print(" last name: " + personArr[1]);
//            System.out.print(" age: " + personArr[2]);
//
//
//    }//end person
//
//}


//    public static void main(String[] args) {
//        Scanner inp = new Scanner(System.in);
//        int size = inp.nextInt();
//        int[] arr = new int[size];
//        for(int i=0 ;i<=size-1;i++) {
//            arr[i]=inp.nextInt();
//        }
//        //#2 Your code here
//        for (int j = 0; j < arr.length; j++) {
//            isPos(arr[j]);
//        }
//    }
//
//    public static void isPos(int num)
//    {
//        //#1 your code here
//        if (num > 0) {
//            System.out.println("positive");
//        }
//        else {
//            System.out.println("not positive");
//        }
//    }

    public static void main(String[] args) {
//        String[] some_array = {"a", "Foo", "bar", "foo", "bla"};
//        String some_string = "foo";
        //System.out.println(countWords(some_array, some_string));
        //System.out.println(c_profits(100, 200));
        //System.out.println(waterTax(151));

//        Scanner inp = new Scanner(System.in);
//        int size = inp.nextInt(), n = inp.nextInt();
//        int[] arr = new int[size];
//        for(int i=0 ;i<=size-1;i++)
//        {
//            arr[i]=inp.nextInt();
//        }
//        add_to_r(arr, n);

//        Scanner scan = new Scanner(System.in);
//        int size = scan.nextInt();
//        int[] nums = new int[size];
//        for(int i=0; i < size; i++){
//            nums[i] = scan.nextInt();
//        }
//        printUniqueNumbers(nums);

//        Scanner scan = new Scanner(System.in);
//        int size = scan.nextInt();
//        int[] nums = new int[size];
//        for(int i=0; i < size; i++){
//            nums[i] = scan.nextInt();
//        }
//        printUniqueNumbers(nums);


//        Scanner scan = new Scanner(System.in);
//        int size = scan.nextInt();
//        String[] words = new String[size];
//        for(int i=0; i < size; i++){
//            words[i] = scan.next();
//        }
//        printUniqueWords(words);

//        String s1 = "12345";
//        String s2 = "abcde";
//        String s3 = "wooden-spoon";
//        //System.out.println(mergeStrings(s1, s2));
        //System.out.println( uniqueChars("wooden-spoon") ) ;

        //System.out.println(wordCount("This method gets a string and returns the word count of that string ."));

        //reverse("hello");

        //System.out.println(clean("one two three","two"));

        //System.out.println(limit("abcd", 2));

        //System.out.println(at3("longword","foo"));

//        int[] arr = {1,2,55,3,4,5,44};
//        double[] arr1 = {1.1,2.3,5.0,3,4,5,44};
//        System.out.println(findMax(arr1));

        //System.out.println(extractNum("aa2aa3"));

        //palindrome("level");

        //System.out.println(isError("error three four error"));
        int[] a = {1, 2, 3, 4, 5, 6};
        int[] b = {7, 8, 9, 10, 11, 12};
//
//        System.out.println(Arrays.toString(mergR(a, b)));


//        String[] r1 = {"f", "o", "o"};
//        String[] r2 = {"b","a","r"};
//        System.out.println(combineRs(r1, r2));

        //System.out.println(validateTask(true,2,1));

        //System.out.println(appearsTwice( "java", "java Javascript is fun!"));

        //System.out.println(Arrays.toString(reverse(r1)));

//        int[] arr = {7,8,9};
//        System.out.println(isSort(arr));

//        ArrayList<Integer> list = new ArrayList<>();
//        list.add(-1);
//        list.add(-4);
//        list.add(1);
//        list.add(0);
//        list.add(2);
//
//        System.out.println(appendPosSum(list));

//        timesTwo(list);

//        ArrayList<Boolean> list = new ArrayList<>();
//        list.add(true);
//        list.add(false);
//        list.add(true);
//
//        repeatAL(list);

        //System.out.println(isPalindrome("Civ ic"));

        //System.out.println(isAnagram("rats", "rats"));

        //System.out.println(Arrays.toString(addElements(a, b)));

        //System.out.println(Arrays.toString(do_switch(a)));

//        int[] i = new int[13];
//        i = populate(i);
//        System.out.println(Arrays.toString(i));

//        String[] arr = {"2", "a", "a", "a", "b", "b"}; // "1","2","aa"',"1"
//        System.out.println(getDup(arr));

        System.out.println(coverString("java methods", "me"));

//        ArrayList<Integer> arr = new ArrayList<>();
//        arr.add(7);
//        arr.add(3);
//        arr.add(1);
//        arr.add(2);
//        arr.add(6);
//        int times = refuel_times(arr, 3);
//        System.out.print(times);//should output 5

    }

    public static int countWords(String[] arr, String word) {
        int count = 0;
        for (String each : arr) {
            if (each.equalsIgnoreCase(word)) {
                count++;
            }
        }
        return count;
    }

    public static String c_profits(int buyPrice, int sellPrice) {
        //your code here
        String result = "";
        if (buyPrice < sellPrice) {
            result = "profit";
        } else if (buyPrice > sellPrice) {
            result = "loss";
        } else {
            result = "no loss";
        }
        return result;
    }


    public static double waterTax(double units) {
        double bill = 0.0;
        //your code here
        if (units <= 50) {
            bill = units * 0.6;
        } else if (units > 50 && units <= 100) {
            bill = units * 0.9;
        } else if (units > 100 && units <= 150) {
            bill = units * 0.9 + 50;
        } else if (units > 150) {
            bill = units * 0.9 + 100;
        }
        //end your code here
        return bill;
    }//end waterTax


    public static void next3(int num) {
        for (int i = num + 1; i <= num + 3; i++) {
            System.out.print(i + " ");
        }
    }

    public static void add_to_r(int[] r, int n) {
        //create new array with one more position.
        int[] new_r = new int[r.length + 1];
        for (int i = 0; i < r.length; i++) {
            new_r[i] = r[i];
            new_r[new_r.length - 1] = n;
        }
        System.out.println(Arrays.toString(new_r));
    }

    public static void printUniqueNumbers(int[] nums) {
        //WRITE YOUR CODE HERE

        for (int i = 0; i < nums.length; i++) {
            int count = 0;
            int num = nums[i];
            for (int each : nums) {
                if (num == each) {
                    count++;
                }
            }
            if (count == 1) {
                System.out.println(num);
            }
        }

    }

    public static void printUniqueWords(String[] words) {
        //WRITE YOUR CODE HERE
        /*
java
code
python
code
rust
code
rust
         */
        for (int i = 0; i < words.length; i++) {
            int count = 0;
            String word = words[i];
            for (String each : words) {
                if (word.equals(each)) {
                    count++;
                }
            }
            if (count == 1) {
                System.out.println(word);
            }
        }
    }

    public static String mergeStrings(String one, String two) {
        String result = "";
        for (int i = 0; i < one.length() || i < two.length(); i++) {
            if (i < one.length()) {
                result += one.charAt(i);
            }
            if (i < two.length()) {
                result += two.charAt(i);
            }
        }
        return result;
    }

    public static String uniqueChars(String str) {
        //TODO: write your below
        String noDup = "";
        for (int i = 0; i < str.length(); i++) {
            String s = str.substring(i, i + 1);
            if (!noDup.contains(s)) {
                noDup += s;
            }
        }
        return noDup;
    }

    public static int wordCount(String words) {
        int count = 0;
        String[] arr = words.split(" ");
        for (int i = 0; i < arr.length; i++) {
            count++;
        }
        return count;
    }

    public static void reverse(String input) {
        String[] arr = input.split("");
        String[] reverse = new String[arr.length];
        int k = 0;
        for (int i = arr.length - 1; i >= 0; i--) {
            reverse[k] = arr[i];
            k++;
        }
        for (String each : reverse) {
            System.out.print(each);
        }
    }

    public static String reverse2(String input) {
        String reverse = "";
        for (int i = input.length() - 1; i >= 0; i--) {
            char ch = input.charAt(i);
            reverse += ch;
        }
        return reverse;
    }

    //"he said bla bla bla","bla"
    public static String clean(String text, String badWord) {
        String cleaned = "";
        for (int i = 0; i <= text.length() - badWord.length(); i++) {
            String word = text.substring(i, i + badWord.length());

            if (word.equalsIgnoreCase(badWord.toLowerCase())) {
                cleaned = text.replace(word, "");
            }
        }
        return cleaned;
    }

    public static String limit(String text, int maxLength) {
        String lim = "";
        for (int i = 0; i < maxLength; i++) {
            lim = text.substring(0, maxLength);
        }

        return lim;
    }

    public static String at3(String target, String word) {
        String str1 = target.substring(0, 3);
        String str2 = target.substring(3);
        String result = str1 + word + str2;
        return result;
    }

    public static String biggerS(String a, String b) {
        if (a.length() > b.length()) {
            return a;
        } else
            return b;
    }

    public int findMax(int[] arr) {
        int max = arr[0];
        for (int each : arr) {
            if (each > max) {
                max = each;
            }
        }
        return max;
    }

    public double findMax(double[] arr) {
        double max = arr[0];
        for (double each : arr) {
            if (each > max) {
                max = each;
            }
        }
        return max;
    }

    public static String extractNum(String s) {
        String result = "";

        for (int i = 0; i < s.length(); i++) {
            char ch = s.charAt(i);
            if (Character.isDigit(ch)) {
                result += ch;
            }
        }
        return result;
    }

    public static void reverseString(String line) {
        String result = "";
        for (int i = line.length() - 1; i >= 0; i--) {
            char ch = line.charAt(i);
            result += ch;
        }
        System.out.println(result);
    }

    public static void palindrome(String line) {
        String result = "";
        for (int i = line.length() - 1; i >= 0; i--) {
            char ch = line.charAt(i);
            result += ch;
        }
        if (result.equalsIgnoreCase(line)) {
            System.out.println("palindrome");
        } else {
            System.out.println("fake");
        }
    }

    public static boolean isError(String line) {
        boolean error = false;
        String err = line.substring(0, 5);
        if (err.contains("error".toLowerCase())) {
            error = true;
        }
        return error;
    }

    public static ArrayList<Integer> test() {
        //write code here
        ArrayList<Integer> list = new ArrayList<>();
        return list;
    }

    public static int[] mergR(int[] a, int[] b) {
        int[] c = new int[a.length + b.length];
        int k = 0;
        for (int each : a) {
            c[k] = each;
            k++;
        }
        for (int each : b) {
            c[k] = each;
            k++;
        }
        return c;
    }//end mergR

    public static String combineRs(String[] r1, String[] r2) {
        ArrayList<String> list = new ArrayList<>();
        for (String each : r1) {
            list.add(each);
        }
        for (String each : r2) {
            list.add(each);
        }
        String res = String.join("", list);
        return res;
    }

    public static boolean validateTask(boolean notEmpty, int taskId, int currentId) {
        /*
        We have a to do list app, every time a user adds a task - a few things needed to be checked.
It should not be empty. boolean parameter needs to be true
taskId should be only 1 greater than currentID. for example if we have
7 tasks(currentId is 7) so the next task id is 8 , it can't be 10.
         */
        int dif = taskId - currentId;
        if (notEmpty == true && dif == 1) {
            return true;
        } else
            return false;
    }

    public static boolean appearsTwice(String target, String sentence) {
        //write your codes here:
        int count = 0;
        for (int i = 0; i <= sentence.length() - target.length(); i++) {
            String str = sentence.toLowerCase().substring(i, i + target.length());
            if (str.contains(target.toLowerCase())) {
                count++;
            }
        }
        if (count == 2) {
            return true;
        } else
            return false;
    }

    /**
     * Reverses the order of the elements in the specified array
     *
     * @param arr
     * @return array of strings
     */
    public static String[] reverse(String[] arr) {
        //write your codes here
        String[] array = new String[arr.length];
        int k = 0;
        for (int i = arr.length - 1; i >= 0; i--) {
            array[k] = arr[i];
            k++;
        }
        return array;
    }

    public static boolean isSort(int[] nums) {
        int[] sorted = new int[nums.length];
        for (int i = 0; i < nums.length; i++) {
            sorted[i] = nums[i];
        }
        Arrays.sort(sorted);
        return Arrays.equals(sorted, nums);
    }

    public static ArrayList<String> combineAL(ArrayList<String> wordList1, ArrayList<String> wordList2) {
        ArrayList<String> list = new ArrayList<>();
        for (String each : wordList1) {
            list.add(each);
        }
        for (String each : wordList2) {
            list.add(each);
        }
        return list;
    }

    public static ArrayList<Integer> appendPosSum(ArrayList<Integer> lst) {
        ArrayList<Integer> list = new ArrayList<>();
        Integer sum = 0;
        System.out.println(lst);
        for (Integer each : lst) {
            if (each > 0) {
                list.add(each);
                sum += each;
            }
        }
        list.add(sum);
        return list;
    }

    public static void timesTwo(ArrayList<Integer> nums) {

        for (int i = 0; i < nums.size(); i++) {
            Integer each = nums.get(i);
            nums.set(i, each * 2);
        }
        System.out.println(nums);
    }

    public static void repeatAL(ArrayList<Boolean> values) {
        System.out.println(values);
        values.addAll(values);
        System.out.println(values);
    }

    public static boolean isPalindrome(String check) {
        String result = "";
        if (check.contains(" ")) {
            check = check.replace(" ", "");
        }
        for (int i = check.length() - 1; i >= 0; i--) {
            char ch = check.charAt(i);
            result += ch;
        }
        if (result.equalsIgnoreCase(check)) {
            return true;
        } else
            return false;
    }

    public static boolean isAnagram(String word1, String word2) {

        if (word1.contains(" ") || word2.contains(" ")) {
            word1 = word1.replace(" ", "");
            word2 = word2.replace(" ", "");
        }

        word1 = word1.toLowerCase();
        word2 = word2.toLowerCase();

        if (word1.equals(word2)) {
            return false;
        }

        char[] arr1 = word1.toCharArray();
        char[] arr2 = word2.toCharArray();

        Arrays.sort(arr1);
        Arrays.sort(arr2);

        if (arr1.length != arr2.length) {
            return false;
        }

        for (int i = 0; i < arr1.length; i++)
            if (arr1[i] != arr2[i])
                return false;

        return true;
    }

    public static int[] addElements(int[] ints1, int[] ints2) {
        int[] ints3 = new int[ints1.length];
        int k = 0;
        for (int i = 0; i < ints1.length; i++) {
            ints3[k] = ints1[i] + ints2[i];
            k++;
        }
        return ints3;
    }

    public static int[] do_switch(int[] i) {
        int first = i[0];
        i[0] = i[i.length - 1];
        i[i.length - 1] = first;
        return i;
    }

    public static int[] populate(int[] r) {
        int[] arr = new int[r.length];
        for (int i = 0; i < r.length; i++) {
            arr[i] = i + 1;
        }
        return arr;
    }

    public static String extractNum2(String s) {
        String result = "";
        for (int i = 0; i < s.length(); i++) {
            char ch = s.charAt(i);
            if (Character.isDigit(ch)) {
                result += ch;
            }
        }
        return result;

    }

    public static int getDup(String[] r) {
        int totalCount = 0;
        for (int i = 0; i < r.length; i++) {
            int count = 0;
            for (int j = 0; j < r.length; j++) {
                if (r[i].equalsIgnoreCase(r[j])) {
                    count++;
                }
            }
            if (count > 1) {
                totalCount++;
            }
        }
        return totalCount;
    }

    public static int refuel_times(ArrayList<Integer> deliveries, int max_fuel) {
        int sum = 0;
        for (int each : deliveries) {
            sum += each;
        }
        return sum / max_fuel + (sum % max_fuel != 0 ? 1 : 0);
    }

    public static String coverString(String main, String coverME) {
        String result = "";
        for (int i = 0; i < main.length() - coverME.length(); i++) {
            String str = main.substring(i, i + coverME.length());
            if (str.contains(coverME)) {
                result = main.replace(coverME, "["+coverME+"]");
            } else if (!main.contains(coverME)) {
                result = "[" + main + "]";
            }
        }
        return result;
    }
}